export const config = {
  stockSymbols: ["VED", "HAL", "MRF", "TSLA"], // List of stock symbols
  initialBalance: 10000, // Starting balance for the bot
  updateInterval: 5000, // Update every 5 seconds
};
